<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="loaded" actionBarHidden="true">
    <StackLayout>
        <Image src="~/images/icons/logo_nobg_192.png" className="splash-logo" />
        <Label text="{{ splashMessage }}" className="message" textWrap="true" />
        <ActivityIndicator busy="{{ isLoading }}" />
    </StackLayout>
</Page>
