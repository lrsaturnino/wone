<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="loaded">
    <Page.actionBar>
        <ActionBar title="Selecione a Categoria">
            <android>
                <NavigationButton icon="res://ic_arrow_back" tap="goBack" />
            </android>
        </ActionBar>
    </Page.actionBar>
    <GridLayout rows="auto, *, auto, *, auto, *" columns="*">
        <Label text="Gastos Básicos" row="0" className="subtitle-left"/>
        <ListView items="{{ basicSubCategoryList }}" row="1" className="list-view">
            <ListView.itemTemplate>
                <GridLayout columns="*">
                    <Label text="{{ SubCategoryName }}" col="0" tap="selectSubCategory"/>
                </GridLayout>
            </ListView.itemTemplate>
        </ListView>
        <Label text="Gastos Extras" row="2" className="subtitle-left"/>
        <ListView items="{{ extraSubCategoryList }}" row="3" className="list-view">
            <ListView.itemTemplate>
                <GridLayout columns="*">
                    <Label text="{{ SubCategoryName }}" col="0" tap="selectSubCategory"/>
                </GridLayout>
            </ListView.itemTemplate>
        </ListView>
        <Label text="Investimentos" row="4" className="subtitle-left"/>
        <ListView items="{{ investimentSubCategoryList }}" row="5" className="list-view">
            <ListView.itemTemplate>
                <GridLayout columns="*">
                    <Label text="{{ SubCategoryName }}" col="0" tap="selectSubCategory"/>
                </GridLayout>
            </ListView.itemTemplate>
        </ListView>
    </GridLayout>
</Page>