<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="loaded">
    <Page.actionBar>
        <ActionBar title="Selecione o parcelamento">
            <ActionBar.actionItems>
            	<ActionItem text="Pronto" ios.position="right" tap="goBack" />
            </ActionBar.actionItems>
        </ActionBar>
    </Page.actionBar>
  	<GridLayout rows="*" columns="*">
        <ListPicker id="installmentList" items="{{ installmentList }}" selectedIndex="0" row="0" />    
 	</GridLayout>
</Page>