<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="loaded">
    <Page.actionBar>
        <ActionBar title="Selecione a data">
            <ActionBar.actionItems>
            	<ActionItem text="Pronto" ios.position="right" tap="goBack" />
            </ActionBar.actionItems>
        </ActionBar>
    </Page.actionBar>
  	<GridLayout rows="*" columns="*">
        <DatePicker id="datepicker" day="{{ day }}" month="{{ month }}" year="{{ year }}" row="0" />
 	</GridLayout>
</Page>